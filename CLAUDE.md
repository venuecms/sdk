# Project Rules

## Versioning

`@venuecms/sdk-next` version MUST match the `@venuecms/sdk` version it depends on.
Example: if `sdk-next` peer-depends on `@venuecms/sdk` `^1.5.1`, then `sdk-next` version is `1.5.1`.
Bump both together whenever the sdk dependency changes.
